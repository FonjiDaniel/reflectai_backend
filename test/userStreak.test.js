import request from "supertest";
import { app } from "../app"; // Adjust path to your Express app

// describe("Reset Streaks API", () => {
//   it("should reset streaks successfully", async () => {
//     const response = await request(app).post("/api/v1/reset-streaks");
//     expect(response.status).toBe(200);
//     expect(response.body.message).toBe("Streaks reset successfully");
//   });
// });
